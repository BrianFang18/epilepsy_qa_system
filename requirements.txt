# Legacy compatibility entry point only; pyproject.toml is the authoritative dependency source.
# The explicit pins below keep legacy scanners aware of the management-plane stack.
alembic==1.19.1
argon2-cffi==25.1.0
minio==7.2.20
psycopg[binary]==3.3.5
python-multipart==0.0.32
SQLAlchemy==2.0.52

# Production/runtime install, including the project and retrieval dependencies.
.[runtime]
